import React from 'react';
import Map from '../Map';

const WalkPage = ({ data }) => {
  return (
    <div className="map-page-container">
      <h1>walk</h1>
    </div>
  );
};

export default WalkPage;